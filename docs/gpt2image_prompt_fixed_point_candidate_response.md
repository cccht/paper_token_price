# GPT2Image Prompt: Fixed-Point Candidate Response Diagram (Figure 4)

## Target

Replace the current `fixed_point_candidate_response.pdf` (generated by matplotlib patches in
`build_reference_aligned_figures.py::fixed_point_map`) with a professional academic schematic.

## Prompt

```
Create a professional academic schematic diagram (landscape orientation,
16:9 or similar wide aspect) for a journal paper. The diagram illustrates
a "three-layer fixed-point candidate response system" in an AI inference
service pricing game. Clean, modern, Nature-journal style. No AI-generated
text artifacts.

═══ COLOR PALETTE ═══
- Upper layer (Platform): a muted sky blue #D7EAF7 box with dark text
- Middle layer (Brokers): a muted sage green #DDEFD5 box with dark text
- Lower layer (Users): a muted rose pink #F8D7DA box with dark text
- Feedback loop: medium gray #777777 lines and text
- Background: white
- All text: dark charcoal #333333, sans-serif font, 10-12pt

═══ LAYOUT (left to right, equal horizontal spacing) ═══

TOP ROW — three rounded-rectangle boxes, horizontally aligned:

  [Box 1]  left third:
    Title: "Upper Map"
    Subtitle: "fP(w | x)"
    Small note below: "(Platform selects wholesale prices)"

  [Box 2]  center third:
    Title: "Middle Map"
    Subtitle: "fI(p,g | w,s)"
    Small note below: "(Brokers choose retail prices & capacity)"

  [Box 3]  right third:
    Title: "Lower Map"
    Subtitle: "fU(s | p,g,q)"
    Small note below: "(Users migrate -> shares, demand, QoS)"

ARROWS between boxes:

  - Box 1 -> Box 2:  A clean horizontal arrow labeled "w"
    (wholesale prices flow right)
  - Box 2 -> Box 3:  A clean horizontal arrow labeled "p, g"
    (retail prices and capacity flow right)

═══ FEEDBACK LOOP (bottom half) ═══

A curved U-shaped or semi-circular feedback arc flowing from Box 3
(right side) down and looping back to Box 1 (left side):

  - Arrows go: Box 3 bottom corner -> downward -> horizontal across
    bottom -> upward -> Box 1 bottom corner
  - Label on the bottom horizontal segment: "shares and effective
    demand" (in gray, medium font)
  - The feedback path should use thin gray arrows with gentle
    rounded corners (NOT sharp right angles)
  - A small annotation next to the path: "QoS feedback"

═══ CONVERGENCE GATE (below the feedback loop) ═══

A small badge or callout at the very bottom, centered:
  "Convergence Gate"
  With subtitle text below it in smaller font:
  "A candidate response is accepted only when solver-based regret
   is sufficiently small."

This could be styled as:
  - A thin bordered rectangle with light gray background (#F5F5F5)
  - OR a simple text block with a gray horizontal rule above it

═══ STYLE REQUIREMENTS ═══
  - Boxes: rounded corners (radius ~6px), thin border (#CCCCCC),
    subtle shadow optional but not necessary
  - Arrows: clean directional arrows, line width ~2px, arrowheads
    proportional
  - Typography: crisp, no decorative fonts. Use a consistent sans-serif
    (Inter, Helvetica, or similar). Math symbols in italic.
  - No emoji, no icons, no clip art
  - No background grid or decorative elements
  - The entire diagram should feel like a refined control-flow or
    systems-engineering schematic

═══ DIMENSIONS ═══
  - Output as a crisp vector graphic
  - Width approximately 7.2 inches, height approximately 3.2 inches
  - Suitable for direct inclusion as PDF in a LaTeX paper
```

## LaTeX Figure Context

In the paper, this figure appears as:

```latex
\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{figures/reference_aligned/fixed_point_candidate_response.pdf}
\caption{三层候选响应的固定点视角。上层映射、中层最优响应和下层logit SUE--QoS
固定点共同构成复合响应系统；数值接受准则是solver-based regret足够小。}
\label{fig:fixed_point_candidate_response}
\end{figure}
```
